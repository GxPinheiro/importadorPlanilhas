const csv = require('csv-parser');
const fs = require('fs');
const moment = require('moment');

module.exports = {
    async getColumns(req, res) {
        console.log("here");
        res.json("end");
    },
};